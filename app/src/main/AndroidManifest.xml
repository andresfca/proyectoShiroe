<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andres.shiroestore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.login.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".activity.admin.AdminMainViewActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.login.SingInActivity" />
        <activity android:name=".activity.admin.ProductDetailActivity" />
        <activity android:name=".activity.admin.CreateProuctActivity" />
        <activity android:name=".activity.admin.ProductEditActivity" />
        <activity android:name=".activity.user.UserMainActivity" />
        <activity android:name=".activity.user.StoreActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>